import createTicketHandler, {
  closeTicketHandler,
  priorityTicketHandler,
  pinTicketHandler,
  deleteTicketHandler,
} from '../../../handlers/ticketButtons.js';

export default [
  createTicketHandler,
  closeTicketHandler,
  priorityTicketHandler,
  pinTicketHandler,
  deleteTicketHandler,
];
