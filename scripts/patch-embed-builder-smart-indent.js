// Intentionally left as a no-op.
// Embed Builder text must pass through exactly as entered by the user.
// Do not inject smart indentation, wrapping, or emoji-specific formatting
// into preview, posted embeds, or modified embeds.
console.log('[EMBED_BUILDER_SMART_INDENT] disabled; preserving exact editor text');
